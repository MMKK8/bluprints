blueprint:
  name: "IKEA SOMRIG (ZHA) - 6 Acciones (Fix Dispositivo)"
  description: "Control total para el mando IKEA SOMRIG. Si no aparecía el dispositivo, esta versión usa un filtro de modelo más amplio."
  domain: automation
  input:
    somrig_device:
      name: Mando SOMRIG
      description: Selecciona el dispositivo IKEA SOMRIG.
      selector:
        device:
          filter:
            - integration: zha
              model: "SOMRIG shortcut button"  # Filtro reducido para mayor compatibilidad
            - integration: zha
              model: "IKEA of Sweden SOMRIG shortcut button"
    # ... (el resto de las acciones se mantienen igual)
